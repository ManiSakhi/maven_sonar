pipeline {
    agent any

    environment {
        MAVEN_HOME = tool 'maven'
    }

    stages {
        stage('Maven Build') {
            steps {
                script {
                    sh "$MAVEN_HOME/bin/mvn clean package sonar:sonar"
                }
            }
        }
    }
}
